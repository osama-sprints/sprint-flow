Hi {first_name} — {bot_handle} here, checking in a few days into running **{cohort_name}**.

- **Is the calendar populated?** `what's scheduled?` shows what the cohort will see. If it's empty, `schedule sprint planning for {cohort_name} <day> <time>` gets you started — I'll confirm the exact time before saving.
- **Sprint open?** `open sprint "Sprint 1" for {cohort_name} from <date>` — standups and reviews attach to it.
- **Changes are cheap.** `move the retro to Friday 3pm` or `cancel Thursday's Q&A` — same confirmation step, full audit trail.

Anything about scheduling that felt clunky? Tell me here. Fellow leads: {lead_handles}.
